import { TextParagraphPipe } from './text-paragraph.pipe';

describe('TextParagraphPipe', () => {
  it('create an instance', () => {
    const pipe = new TextParagraphPipe();
    expect(pipe).toBeTruthy();
  });
});
